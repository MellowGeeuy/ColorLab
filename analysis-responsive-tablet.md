# วิเคราะห์ความพร้อมด้าน Responsive — แท็บเล็ต (768–1279px)

> **ขอบเขต:** จอกว้าง 768–1279px (แท็บเล็ตแนวตั้งและแนวนอน)
> **คู่มือพี่น้อง:** [`analysis-responsive-mobile.md`](./analysis-responsive-mobile.md) — 320–767px
> **สถานะโค้ดที่ตรวจ:** commit `2754978` · CSS 13,057 บรรทัด / JS 13,068 บรรทัด
> **วันที่ตรวจ:** 2026-09-11

---

## 0. สถานะการแก้ไข (อัปเดต 2026-09-11)

เฟส A ลงมือแก้แล้ว · วัดซ้ำด้วย harness เดิม:

| หน้า | 768 | 834 | 900 | 1024 | 1100 | 1180 | 1280 |
|---|---|---|---|---|---|---|---|
| หน้าแรก | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| Workspace | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| Layout | ✅ **93→0** | ✅ **27→0** | ✅ | ✅ | ✅ | ✅ | ✅ |
| Component Style | ✅ **143→0** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

| ข้อ | สถานะ | หมายเหตุ |
|---|---|---|
| T0-1 หลุมยุบ 768–899px | ✅ แก้แล้ว | `max-width: 760px` → `899px` |
| T0-2 แผงคลังล้น 42px | ✅ แก้แล้ว | **ต้นเหตุจริงต่างจากที่วินิจฉัยไว้ — ดู §3 T0-2** |
| T1-1 `.ui-navbar` ล้น | ✅ แก้แล้ว | ใช้วิธีให้เลื่อนดูได้ ไม่ใช่แฮมเบอร์เกอร์ — ดู §4 T1-1 |
| T1-2 tap target | ✅ แก้แล้ว | `any-pointer: coarse` ครอบแท็บเล็ตอัตโนมัติตามที่คาดไว้ |
| T1-3 จุดล้นค้าง 900–1280px | ✅ หมดไปพร้อม T0-2/T1-1 | |
| T2-1 · T2-2 · T2-3 | ⬜ ยังไม่แก้ | ต้องทำพร้อมงาน breakpoint ทั้งระบบ |

---

## 1. บทสรุปผู้บริหาร

ช่วงแท็บเล็ตเป็นช่วงที่ ColorLab **ดูแลไว้ดีที่สุดและแย่ที่สุดพร้อมกัน** — ขึ้นอยู่กับว่าเปิดหน้าไหน

| หน้า | 768 | 834 | 900 | 1024 | 1100 | 1180 | 1280 |
|---|---|---|---|---|---|---|---|
| หน้าแรก | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| Workspace | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| **Layout** | 🔴 **93px** | 🔴 **27px** | 🟠 | 🟠 | 🟠 | 🟠 | 🟠 |
| **Component Style** | 🟠 **143px** | 🟠 | 🟠 | 🟠 | ✅ | 🟠 | 🟠 |

หน้าแรกกับ Workspace **ผ่านสะอาดทุกความกว้างในช่วงนี้** — งานที่ทำไว้ที่ breakpoint 1180px
(ยุบ inspector เป็น drawer) กับ 900px ได้ผลจริง

แต่มีปัญหาเฉพาะตัวที่แท็บเล็ตเจอหนักกว่ามือถืออีก:

> **หน้า Layout พังที่ 768–899px ทั้งที่ผ่านสะอาดในช่วง 480–720px**
> คือ "หลุมยุบ" กลางสเปกตรัม — จอใหญ่ขึ้นแต่กลับใช้งานได้แย่ลง

| ระดับ | จำนวน | ผลกระทบ |
|---|---|---|
| 🔴 **T0** | 2 | หน้า Layout ใช้ไม่ได้ในช่วงแท็บเล็ตแนวตั้ง |
| 🟠 **T1** | 3 | คอมโพเนนต์ในแคตตาล็อกล้น / ปุ่มเล็กเกินสำหรับนิ้ว |
| 🟡 **T2** | 3 | สมมติฐานเรื่องอุปกรณ์ผิด — แท็บเล็ตถูกปฏิบัติเหมือนเดสก์ท็อปจอแคบ |

**ประเมินแรงงาน:** T0 ≈ 1 วัน · T0+T1 ≈ 3 วัน · ครบทั้งหมด ≈ 5 วัน
(งานส่วนหนึ่งใช้ผลลัพธ์ร่วมกับเฟส 1–2 ของฝั่งมือถือ — ทำพร้อมกันประหยัดกว่า)

---

## 2. หลุมยุบ 768–899px — ปัญหาเด่นของเอกสารฉบับนี้

### 2.1 ภาพรวมที่วัดได้

พิกเซลที่ `.app` ล้นออกไปแล้วเข้าถึงไม่ได้ (หน้า Layout):

```
320px ▇▇▇▇▇▇▇▇▇▇▇▇  119
360px ▇▇▇▇▇▇▇▇       79
390px ▇▇▇▇▇          49
412px ▇▇▇            27
480px                 0   ← ผ่าน
540px                 0
600px                 0
640px                 0
680px                 0
720px                 0
768px ▇▇▇▇▇▇▇▇▇      93   ← พังใหม่!
834px ▇▇▇            27
900px                 0   ← ผ่านอีกครั้ง
1024px                0
```

**นี่ไม่ใช่ความเสื่อมแบบค่อยเป็นค่อยไป แต่เป็นการถดถอย** — เว็บทำงานได้ที่ 720px
แล้วพังที่ 768px ซึ่งเป็นความกว้างแท็บเล็ตแนวตั้งที่พบบ่อยที่สุด (iPad รุ่นมาตรฐาน)

### 2.2 ต้นเหตุ: breakpoint พลาดไป 8px

```css
/* assets/css/page-specific/layout.css:1092 */
@media (max-width: 1100px) {
  .lay { grid-template-columns: var(--lib-w, 11rem) minmax(0, 1fr); }
  .lay-insp { grid-column: 1 / -1; ... }
}

/* assets/css/page-specific/layout.css:1097 */
@media (max-width: 760px) {           /* ← ต่ำกว่า 768 อยู่ 8px พอดี */
  .lay { grid-template-columns: minmax(0, 1fr); }
  .lay-dock { flex-wrap: wrap; justify-content: center; ... }
  .lay-dock__summary { display: none; }
}
```

ที่ **768px** จอตกอยู่ระหว่างสองกฎนี้ — ได้กฎ `1100px` (2 คอลัมน์) แต่ **ไม่ได้กฎ `760px`**
ที่เป็นตัวยุบ dock ให้ wrap และซ่อน `.lay-dock__summary`

ผลที่วัดได้ที่ 768px:

| element | ช่องที่มี | เนื้อหาต้องการ | ล้น |
|---|---|---|---|
| `.lay-dock` | 558px | 668px | **110px** |
| `.lay-main` | 592px | 685px | **93px** |
| `#lay-library-panel` | 175px | 217px | **42px** |
| `.lay` / `.app` | 768px | 861px | **93px** |

ยืนยันด้วยภาพ: ที่ 768px แถบเครื่องมือลอย (`.lay-dock`) ถูกตัดขอบขวา — ปุ่มตัวสุดท้ายหายไปครึ่งตัว

**เลข `760` มาจากไหน?** เป็นค่าที่ถูกเลือกเพื่อ "ต่ำกว่า 768 นิดหน่อย"
ซึ่งเป็นวิธีคิดที่ถูกต้องสำหรับ `min-width` (mobile-first) แต่ **กลับด้านสำหรับ `max-width`**
ในระบบ `max-width` ค่าที่ถูกต้องคือ `767px` (คลุมทุกอย่างที่ *ต่ำกว่า* 768) หรือ `899px` (ให้แท็บเล็ตแนวตั้งใช้กฎมือถือ)

### 2.3 วิธีแก้

**แก้ทันที (5 นาที) — ให้แท็บเล็ตแนวตั้งใช้กฎเดียวกับมือถือ**

```css
/* assets/css/page-specific/layout.css:1097
   เปลี่ยน 760 → 899: แท็บเล็ตแนวตั้งควรได้ผังคอลัมน์เดียวเหมือนมือถือ
   เพราะ lib 11rem + inspector 14rem + canvas ที่ใช้งานได้จริง ไม่พอดี 768px */
@media (max-width: 899px) {
  .lay { grid-template-columns: minmax(0, 1fr); }
  .lay-lib { border-inline-end: none; border-block-end: 1px solid var(--color-border); }
  .lay-dock { flex-wrap: wrap; justify-content: center; border-radius: var(--radius-lg); }
  .lay-dock__summary { display: none; }
}
```

**แก้ให้ถูกต้องจริง (4 ชม.) — ให้ dock ปรับตัวตามพื้นที่ ไม่ใช่ตามขนาดจอ**

`.lay-dock` ล้นเพราะมันอยู่ใน `.lay-main` ซึ่งความกว้างขึ้นกับว่าแผงข้างเปิดอยู่กี่แผง
media query ไม่มีทางรู้เรื่องนี้ — **แต่ container query รู้**

```css
/* .lay-main เป็นตัวตั้งต้นของการวัด */
.lay-main {
  container-type: inline-size;
  container-name: canvas-area;
}

/* ยุบตามความกว้างของพื้นที่จริง ไม่ว่าจอจะกว้างเท่าไร
   เปิด/ปิดแผงข้างแล้ว dock ปรับตามทันทีโดยไม่ต้องเขียนกฎเพิ่ม */
@container canvas-area (max-width: 640px) {
  .lay-dock { flex-wrap: wrap; justify-content: center; }
  .lay-dock__summary { display: none; }
}
@container canvas-area (max-width: 420px) {
  .lay-dock__zoom-label { display: none; }
}
```

> Container query รองรับในเบราว์เซอร์หลักทุกตัวตั้งแต่ปี 2023
> และโปรเจกต์นี้ **ยังไม่ได้ใช้เลยแม้แต่จุดเดียว** ทั้งที่มี layout แบบแผงข้างพับได้
> ซึ่งเป็นเคสที่ container query ถูกออกแบบมาแก้โดยตรง

---

## 3. ปัญหา T0 — ต้องแก้

### 🔴 T0-1 · หลุมยุบ 768–899px (ดู §2)

สรุปสั้น: `max-width: 760px` ควรเป็น `max-width: 899px`
พร้อมย้าย `.lay-dock` ไปใช้ container query

### 🔴 T0-2 · แผงคลังคอมโพเนนต์ล้นทุกความกว้าง รวมถึง 1280px

ต่างจาก T0-1 ที่หายไปเมื่อจอกว้างขึ้น — ปัญหานี้ **ไม่หายเลย**

| ความกว้างจอ | `#lay-library-panel` ล้น |
|---|---|
| 768px | 42px |
| 1024px | 42px |
| 1100px | 42px |
| 1180px | 42px |
| 1280px | 42px |

ค่าคงที่ 42px ทุกความกว้าง = **ไม่ใช่ปัญหา responsive แต่เป็นเนื้อหาที่กว้างเกินกล่องโดยสมบูรณ์**
แผงมี `client 175px` แต่เนื้อหาต้องการ `217px` และ `overflow-x` ไม่ใช่ `auto` — เนื้อหาจึงถูกตัดถาวร

`--lib-w` ถูกกำหนดเป็น `13rem` (208px) ที่ `assets/css/page-specific/layout.css:15`
และลดเหลือ `11rem` (176px) ที่ ≤1100px (บรรทัด 1093)

> ⚠️ **แก้ข้อวินิจฉัยเดิม** — ฉบับแรกเดาว่าต้นเหตุอยู่ที่ `.lay-lib__item`
> พอวัดจริงด้วย CDP พบว่า **ผิดจุด** — `.lay-lib__name` มี `min-width: 0` + `ellipsis`
> ครบถ้วนอยู่แล้ว และ `.lay-lib` เองก็มี `min-width: 0` แล้วเช่นกัน

**ต้นเหตุจริง — กล่องสองชั้นระหว่างแผงกับรายการ:**

| element | ช่องที่มี | ต้องการ | `min-width` |
|---|---|---|---|
| `.lay-lib__body` | 175px | **217px** | `auto` ← ตัวการหลัก |
| `.lay-lib__search` | 175px | **193px** | `auto` ← ตัวการรอง |

ทั้งคู่เป็น grid item ของ `.lay-lib__pane` ซึ่งมี `min-width: auto` โดยปริยาย
= "ห้ามแคบกว่า min-content" กล่องจึงกางออกแล้วดัน ellipsis ที่รออยู่ข้างล่างไม่ให้ได้ทำงาน

```css
/* ปลดล็อกที่กล่องกลาง แล้ว ellipsis ที่มีอยู่แล้วจะทำงานเอง */
.lay-lib__body   { min-width: 0; }
.lay-lib__search { min-width: 0; }
.lay-lib__item   { min-width: 0; }
```

**บทเรียน:** `min-width: 0` ที่ปลายทาง (ตัวข้อความ) กับที่ต้นทาง (ตัวแผง) ไม่พอ
ถ้ากล่องชั้นกลางยังล็อกอยู่ — ต้องปลดทั้งสาย และต้องวัดเพื่อรู้ว่าชั้นไหนล็อก

---

## 4. ปัญหา T1 — คอมโพเนนต์ตัวอย่างในแคตตาล็อกเอง

### 🟠 T1-1 · `.ui-navbar` ตัวอย่างไม่ responsive

**อาการที่วัดได้ (หน้า Component Style):**

| ความกว้างจอ | `.ui-navbar__menu` ล้น | `.ui-navbar__link` ถูกตัด |
|---|---|---|
| 360px | (ยุบแล้ว) | 50px |
| **768px** | **143px** | 27px |
| 1024px | 15px | 15px |
| 1280px | ยังล้น | — |

`.ui-navbar__menu` มีช่อง `42px` แต่เนื้อหาต้องการ `185px` ที่ 768px

**ทำไมเรื่องนี้สำคัญกว่าที่ตัวเลขบอก:** นี่คือหน้าที่ **สอนคนอื่นเรื่องการออกแบบ UI**
คอมโพเนนต์ตัวอย่างที่แสดงอยู่คือสิ่งที่ผู้ใช้จะคัดลอกไปใช้ — คอมโพเนนต์ที่ล้นจึงสอนสิ่งที่ผิด

**ต้นเหตุเชิงโครงสร้าง:** `assets/css/component-catalog.css` มี **2,436 บรรทัด**
แต่มี media query จริงเพียง **1 จุด** (`assets/css/component-catalog.css:1532`, `max-width: 640px`)
— อีก 8 จุดเป็น `prefers-reduced-motion` ทั้งหมด

นั่นแปลว่าคอมโพเนนต์ทั้ง 21 ตัวใน `assets/components/` ถูกออกแบบให้ยุบตัวได้ที่จุดเดียว
และไม่มีการดูแลช่วง 641–1279px เลย

**วิธีแก้ — ใช้ container query เพราะคอมโพเนนต์ถูกฝังในกรอบพรีวิวที่ขนาดไม่เท่าจอ:**

**วิธีแก้ที่ใช้จริง** — `.ui-navbar__menu` มี `overflow: hidden` อยู่แล้ว
คือรูปแบบ "ตัดหายถาวร" แบบเดียวกับ P0-1 ของฝั่งมือถือ เปลี่ยนให้เลื่อนดูได้แทน:

```css
/* assets/css/component-catalog.css */
.ui-navbar__menu {
  min-width: 0;
  overflow-x: auto;          /* เดิมเป็น overflow: hidden — ลิงก์ 3 ใน 4 ตัวหายถาวร */
  overflow-y: hidden;
  overscroll-behavior-x: contain;
  scrollbar-width: none;
}
.ui-navbar__menu::-webkit-scrollbar { display: none; }
```

**ทำไมถึงเลือกวิธีนี้แทนแฮมเบอร์เกอร์:** ปุ่มแฮมเบอร์เกอร์ต้องเพิ่ม markup ใน
`assets/components/navbar.html` ซึ่งเป็น **โค้ดที่ผู้ใช้คัดลอกออกไปใช้จริง**
การเพิ่มอิลิเมนต์เข้าไปคือการตัดสินใจเชิงออกแบบแทนผู้ใช้ ส่วนการเปลี่ยน
`overflow: hidden` → `overflow-x: auto` แก้ที่ตัวข้อบกพร่องตรง ๆ โดยไม่แตะสิ่งที่หน้านี้สอน

**งานต่อ (ยังไม่ทำ):** ถ้าต้องการให้แคตตาล็อกสอนเรื่อง responsive ด้วย
ให้ทำ `.ui-preview { container-type: inline-size }` แล้วเพิ่มแฮมเบอร์เกอร์เป็น
variant ใหม่ของ navbar — เป็นการ *เพิ่มบทเรียน* ไม่ใช่แก้ของเดิม

### 🟠 T1-2 · Tap target — แท็บเล็ตคือเครื่องทัช แต่ถูกปฏิบัติเหมือนเดสก์ท็อป

วัดที่ 768px:

| หน้า | < 24px | 24–44px | ปุ่มทั้งหมด |
|---|---|---|---|
| หน้าแรก | **14** | 17 | 58 |
| Workspace | 1 | 11 | 76 |
| **Layout** | 0 | **48** | 111 |
| **Component Style** | 1 | **41** | 89 |

หน้า Layout มีปุ่มที่เล็กกว่า 44px ถึง **48 จุดจาก 111 จุด (43%)** บนอุปกรณ์ที่ควบคุมด้วยนิ้ว

**ทางแก้เป็นตัวเดียวกับฝั่งมือถือ** — ดู `analysis-responsive-mobile.md` §4 P0-3
การใช้ `@media (pointer: coarse)` แทน `max-width` ทำให้แท็บเล็ตได้ประโยชน์อัตโนมัติ
โดยไม่ต้องเขียนกฎซ้ำ **ซึ่งเป็นเหตุผลหลักที่แนะนำ `pointer` แทนความกว้างจอ**

### 🟠 T1-3 · จุดล้นที่ยังค้างอยู่ที่ 900–1280px

| หน้า | 900 | 1024 | 1100 | 1180 | 1280 |
|---|---|---|---|---|---|
| Layout | 2 | 2 | 2 | 2 | 2 |
| Component Style | 1 | 1 | 0 | 1 | 1 |

Layout เหลือ 2 จุดคงที่ = `#lay-library-panel` + `#lay-pane-library` (T0-2 ตัวเดียวกัน นับซ้อนกันสองชั้น)
Component Style เหลือ 1 จุด = `.ui-navbar__menu` (T1-1)

แก้ T0-2 กับ T1-1 แล้ว **ช่วง 900–1280px จะสะอาดทั้งหมด**

---

## 5. ปัญหา T2 — สมมติฐานเรื่องอุปกรณ์ที่ผิด

### 🟡 T2-1 · แท็บเล็ตถูกจัดเป็น "เดสก์ท็อปจอแคบ"

breakpoint ที่ใช้ตัดสินใจเรื่อง layout ทั้งหมดอิงความกว้างอย่างเดียว:

| ค่า | ที่ใช้ | สิ่งที่ตัดสินใจ |
|---|---|---|
| 1500px | `assets/css/app-shell.css:606` | ซ่อน label ปุ่ม command palette |
| 1300px | `assets/css/app-shell.css:611` | ซ่อน `.topbar__status` |
| 1180px | `assets/css/page-specific/workspace.css:370` | inspector → drawer |
| 1100px | `assets/css/page-specific/layout.css:1092` | ยุบเหลือ 2 คอลัมน์ |
| 1080px | `assets/css/app-shell.css:617` | ซ่อน label เมนู |

ปัญหาคือ **iPad Pro 12.9" แนวนอน = 1024px** ซึ่งตกในช่วงที่ inspector กลายเป็น drawer แล้ว
ทั้งที่จอกว้าง 1024px มีพื้นที่พอสำหรับ inspector 280px สบาย ๆ
(1024 − rail 64 − inspector 280 = **680px เหลือให้ canvas** ซึ่งกว้างกว่าที่ 768px ได้อีก)

การตัดสินใจนี้ไม่ผิดร้ายแรง แต่สะท้อนว่า **ไม่มีจุดไหนในโค้ดที่ถามว่า "เครื่องนี้ใช้นิ้วหรือเมาส์"**

```css
/* เสนอ: แยกสองคำถามออกจากกัน
   "พื้นที่พอไหม" ตอบด้วยความกว้าง — "นิ้วหรือเมาส์" ตอบด้วย pointer */

/* พื้นที่ไม่พอ → drawer (ไม่ว่าเครื่องอะไร) */
@media (max-width: 1023px) {
  .inspector { position: fixed; transform: translateX(100%); /* ... */ }
}

/* พื้นที่พอแต่ใช้นิ้ว → คงแผงไว้ แค่ขยายปุ่ม */
@media (min-width: 1024px) and (pointer: coarse) {
  .inspector { position: static; transform: none; }
  /* --control-h-* ถูกขยายให้แล้วจากกฎ pointer: coarse ใน design-system.css */
}
```

### 🟡 T2-2 · ไม่มีการจัดการการหมุนจอ

แท็บเล็ตหมุนบ่อยกว่ามือถือมาก — iPad หมุนระหว่าง **768 ↔ 1024** ซึ่งข้ามถึง 3 breakpoint ปัจจุบัน

`assets/js/modules/sidebar-nav.js:45` และ `assets/js/modules/toc-nav.js:60` ใช้:

```js
if (window.innerWidth <= MOBILE_BREAKPOINT) close();
```

โค้ดนี้อ่านค่า **ณ ตอนคลิกเท่านั้น** — ถ้าผู้ใช้เปิด drawer ตอนแนวตั้งแล้วหมุนเป็นแนวนอน
drawer จะค้างอยู่ในสถานะที่ไม่ถูกต้องจนกว่าจะคลิกลิงก์ครั้งต่อไป

```js
// เสนอ: ให้ระบบบอกเราเองตอนสถานะเปลี่ยน แทนที่จะไปถามตอนคลิก
const mq = window.matchMedia('(max-width: 1023px)');

const syncDrawer = (e) => {
  if (!e.matches) close();       // กว้างพอแล้ว → drawer ไม่ควรเปิดค้าง
};
mq.addEventListener('change', syncDrawer);
syncDrawer(mq);                  // ตั้งค่าให้ถูกตั้งแต่โหลดครั้งแรก
```

### 🟡 T2-3 · `hover: hover` บนแท็บเล็ตไม่ได้แปลว่า "มีเมาส์"

เมื่อทำตามคำแนะนำใน `analysis-responsive-mobile.md` §4 P0-4 ให้ระวังเคสนี้:

iPad ที่ต่อ Magic Keyboard **รายงาน `hover: hover` และ `pointer: fine`** เพราะมีแทร็กแพด
แต่ผู้ใช้ยังแตะจอสลับไปมาได้ตลอดเวลา

```css
/* pointer / hover  = อุปกรณ์ชี้ตำแหน่ง "หลัก"
   any-pointer / any-hover = อุปกรณ์ชี้ตำแหน่ง "ทุกตัวที่มี" */

/* ปลอดภัยที่สุดสำหรับ tap target: ถ้ามีทางแตะได้ ให้ขยายไว้ก่อน */
@media (any-pointer: coarse) {
  :root { --control-h-md: 2.75rem; }
}

/* เอฟเฟกต์ hover ให้เฉพาะตอนอุปกรณ์หลักคือเมาส์ — ไม่งั้น tooltip จะค้างหลังแตะ */
@media (hover: hover) and (pointer: fine) {
  .rail__item:hover .rail__tip { opacity: 1; }
}
```

**หลักการ:** ใช้ `any-pointer: coarse` สำหรับ "ขนาด" · ใช้ `hover: hover` สำหรับ "เอฟเฟกต์"

---

## 6. Breakpoint ช่วงแท็บเล็ตที่เสนอ

ใช้ระบบเดียวกับ `analysis-responsive-mobile.md` §6 โดยช่วงที่เอกสารนี้รับผิดชอบคือ:

| ชื่อ | ช่วง | ควรได้ layout แบบไหน |
|---|---|---|
| `md` | 680–1023px | คอลัมน์เดียว · แผงข้างเป็น drawer · dock ยุบ |
| `lg` | 1024–1279px | 2 คอลัมน์ · inspector คงอยู่ถ้าพื้นที่พอ |

### ตารางเทียบก่อน-หลัง

| breakpoint เดิม | ไฟล์ | ปัญหา | ค่าใหม่ |
|---|---|---|---|
| `760px` | `layout.css:1097` | พลาด 768px ไป 8px | **`899px`** |
| `1100px` | `layout.css:1092` | คร่อม 1024 โดยไม่ตั้งใจ | **`1279px`** |
| `1080px` | `app-shell.css:617` | ใกล้ 1100 จนสับสน | **`1023px`** |
| `1180px` | `workspace.css:370` | ทำให้ iPad Pro เสียแผง | **`1023px`** |
| `900px` | `theory.css:369` ฯลฯ | ใกล้เคียง `md` อยู่แล้ว | **`1023px`** |
| `1300px`, `1500px` | `app-shell.css:606,611` | เป็นการซ่อน label ล้วน คงไว้ได้ | คงเดิม |

---

## 7. แผนลงมือ

### เฟส A — ปิดหลุมยุบ (1 วัน) 🔴

| # | งาน | ไฟล์ | เวลา |
|---|---|---|---|
| 1 | `760px` → `899px` (T0-1) | `layout.css:1097` | 5 นาที |
| 2 | ทดสอบซ้ำที่ 768/834/899/900 | — | 1 ชม. |
| 3 | `min-width: 0` + ตัดคำในแผงคลัง (T0-2) | `layout.css` | 2 ชม. |
| 4 | `.lay-dock` → container query | `layout.css` | 4 ชม. |

**ผลที่ได้:** ช่วง 768–1279px สะอาดทั้งหมดยกเว้น `.ui-navbar`

### เฟส B — คอมโพเนนต์ในแคตตาล็อก (1.5 วัน) 🟠

| # | งาน | เวลา |
|---|---|---|
| 5 | `.ui-preview` เป็น container (T1-1) | 2 ชม. |
| 6 | `.ui-navbar` ยุบเป็นแฮมเบอร์เกอร์ | 3 ชม. |
| 7 | ตรวจคอมโพเนนต์อีก 20 ตัวใน `assets/components/` ทีละตัว | 1 วัน |

### เฟส C — สมมติฐานอุปกรณ์ (1.5 วัน) 🟡

| # | งาน | เวลา |
|---|---|---|
| 8 | `pointer: coarse` / `any-pointer` (T1-2, T2-3) | ใช้ผลร่วมกับเฟส 1 ฝั่งมือถือ |
| 9 | inspector: แยก "พื้นที่พอ" ออกจาก "ใช้นิ้ว" (T2-1) | 4 ชม. |
| 10 | `matchMedia` + `change` แทน `innerWidth` (T2-2) | 3 ชม. |
| 11 | ปรับ breakpoint ตามตาราง §6 | 4 ชม. |

> **ประหยัดแรงงาน:** ข้อ 8 กับข้อ 11 ทับซ้อนกับเฟส 1–2 ของ `analysis-responsive-mobile.md`
> ทำพร้อมกันประหยัดไปได้ประมาณ 1 วัน

---

## 8. Checklist ทดสอบ

### ความกว้างที่ต้องผ่าน

- [ ] **768** · iPad แนวตั้ง — จุดที่พังหนักสุดตอนนี้
- [ ] **834** · iPad Air แนวตั้ง
- [ ] **899 / 900** · คร่อมเส้น breakpoint ใหม่ ต้องไม่กระตุก
- [ ] **1024** · iPad แนวนอน / iPad Pro 11"
- [ ] **1180** · เส้น breakpoint เดิมของ workspace
- [ ] **1279 / 1280** · คร่อมเส้น `lg` → `xl`

### เกณฑ์ผ่าน

- [ ] `.app` ล้น = 0 ทุกความกว้าง
- [ ] ไม่มี container ที่ `scrollWidth > clientWidth` โดย `overflow-x` ไม่ใช่ `auto`/`scroll`
- [ ] `.lay-dock` แสดงปุ่มครบทุกตัวไม่ถูกตัด
- [ ] ปุ่มทุกตัวที่ `any-pointer: coarse` มีด้านสั้น ≥ 44px

### ต้องลองด้วยมือ

- [ ] หมุนจอ 768 ↔ 1024 ขณะเปิด drawer ค้างไว้ — สถานะต้องตามทัน
- [ ] เปิด/ปิดแผงคลังและ inspector สลับกัน — dock ต้องยุบตามพื้นที่จริง (หลังใช้ container query)
- [ ] iPad + Magic Keyboard: tooltip ต้องไม่ค้างหลังแตะจอ
- [ ] ลากคอมโพเนนต์ด้วยนิ้วบน iPad — ต้องรอ P0-2 ฝั่งมือถือก่อน
- [ ] ทดสอบบน iPad จริงอย่างน้อย 1 เครื่อง — Safari มีพฤติกรรม `dvh` ต่างจาก Chrome

---

## 9. อ้างอิงมาตรฐาน

| หัวข้อ | มาตรฐาน |
|---|---|
| Container Queries | CSS Containment Module Level 3 — `container-type`, `@container` |
| ความสามารถของอุปกรณ์ | CSS Media Queries 4 — `pointer`, `hover`, `any-pointer`, `any-hover` |
| ขนาดเป้ากด | WCAG 2.2 SC 2.5.8 (24px, AA) · SC 2.5.5 (44px, AAA) |
| แนวทางแท็บเล็ตของ Apple | Human Interface Guidelines — Layout / Adaptivity |
| Breakpoint ของ Material | Material Design 3 — window size classes (compact / medium / expanded) |

---

## 10. หมายเหตุจากผู้ตรวจ

**สิ่งที่ทำไว้ดีมากในช่วงแท็บเล็ต — เป็นแบบอย่างให้หน้าอื่น:**

- **Workspace ผ่านสะอาดทุกความกว้าง 768–1280px** การยุบ inspector เป็น drawer ที่
  `assets/css/page-specific/workspace.css:370` ทำได้ถูกต้อง พร้อมคู่กฎ `min-width: 1181px`
  ที่ซ่อนปุ่มปิด (บรรทัด 387) — แสดงว่าคิดเรื่องสถานะสองฝั่งของ breakpoint ครบ
- **หน้าแรกผ่านสะอาดทั้งช่วง** — media query เดียวที่ 900px (`assets/css/page-specific/theory.css:369`)
  บวกกับ `.main__inner` ที่ใช้ named grid lines (`assets/css/design-system.css:816`) เพียงพอจริง
- `#tool-rail` ใช้ `overflow-x: auto` — ล้น 110px แต่ผู้ใช้เลื่อนดูได้ **ไม่นับเป็นปัญหา**

**บทเรียนที่ชัดที่สุดจากการตรวจครั้งนี้:**

หน้าที่ผ่านสะอาด (หน้าแรก, Workspace) ใช้ breakpoint **น้อยกว่า** หน้าที่พัง
Workspace มี 7 จุด · หน้าแรกมี 1 จุด · แต่ Layout มี 4 จุดแล้วยังพังในสองช่วง

ความต่างไม่ได้อยู่ที่จำนวน แต่อยู่ที่ **สิ่งที่ breakpoint ตัดสินใจ**
Workspace ใช้ breakpoint ตัดสินว่า "แผงนี้ลอยหรือฝัง" (คำถามเดียว ตอบชัด)
ส่วน Layout ใช้ตัดสินหลายเรื่องพร้อมกันในกฎเดียว — จำนวนคอลัมน์ ตำแหน่ง dock การซ่อน summary
พอเรื่องหนึ่งพลาด 8px เรื่องอื่นพลาดตามหมด

นี่คือเหตุผลที่ §2.3 เสนอ container query — ไม่ใช่เพราะเป็นเทคโนโลยีใหม่
แต่เพราะมันบังคับให้แต่ละองค์ประกอบตอบคำถามของตัวเองจากพื้นที่ที่ตัวเองมีจริง
